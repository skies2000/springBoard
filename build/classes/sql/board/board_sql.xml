<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
    
    <resultMap type="com.spring.board.vo.BoardVo" id="boardVo">
    	<result property="boardType" 	column="BOARD_TYPE"/>
    	<result property="boardNum" 	column="BOARD_NUM"/>
    	<result property="boardTitle" 	column="BOARD_TITLE"/>
    	<result property="boardComment" column="BOARD_COMMENT"/>
    	<result property="codeName" column="CODE_NAME"/>
    	<result property="codeId" column="CODE_ID"/>
    	 
    </resultMap>
    
    <select id="boardList" parameterType="com.spring.board.vo.PageVo" resultMap="boardVo">
        SELECT 
		  BOARD_TYPE
		  ,BOARD_NUM
		  ,BOARD_TITLE
		  ,BOARD_COMMENT
		  ,TOTAL_CNT
		  ,CODE_NAME
		FROM 
		(
		  SELECT
		      	BOARD_TYPE
				,BOARD_NUM
				,BOARD_TITLE
				,BOARD_COMMENT
		      	,ROW_NUMBER() OVER(ORDER BY BOARD_NUM DESC) AS NUMROW
		      	,COUNT(*) OVER() AS TOTAL_CNT
		      	,CASE BOARD_TYPE 
		      	WHEN 'a01' THEN 
		      	(SELECT CODE_NAME FROM COM_CODE WHERE CODE_ID = 'a01' AND CODE_TYPE= 'menu')
		      	WHEN 'a02' THEN 
		      	(SELECT CODE_NAME FROM COM_CODE WHERE CODE_ID = 'a02' AND CODE_TYPE= 'menu')
		      	WHEN 'a03' THEN 
		      	(SELECT CODE_NAME FROM COM_CODE WHERE CODE_ID = 'a03' AND CODE_TYPE= 'menu')
		      	WHEN 'a04' THEN 
		      	(SELECT CODE_NAME FROM COM_CODE WHERE CODE_ID = 'a04' AND CODE_TYPE= 'menu')
				END 
				CODE_NAME
			FROM
				BOARD	
		)
		WHERE NUMROW BETWEEN (#{pageNo}-1)*10 + 1 AND (#{pageNo})*10
    </select>
    
    <select id="boardTotal" parameterType="Integer" resultType="Integer">
		  SELECT
		      	COUNT(*) AS TOTAL_CNT
			FROM
				BOARD	
    </select>
    	
    <select id="boardView" parameterType="com.spring.board.vo.BoardVo" resultMap="boardVo">
    	SELECT
        	BOARD_TYPE
			,BOARD_NUM
			,BOARD_TITLE
			,BOARD_COMMENT
		FROM
			BOARD
    	WHERE 	BOARD_TYPE = #{boardType}
    	AND 	BOARD_NUM  = #{boardNum}
    </select>	
    
    <select id="boardInsert" parameterType="com.spring.board.vo.BoardVo">
        INSERT INTO 
        BOARD
        (BOARD_TYPE
        ,BOARD_NUM
        ,BOARD_TITLE
        ,BOARD_COMMENT
        ,CREATOR
        ,CREATE_TIME
        ,MODIFIER
        ,MODIFIED_TIME
        )
        VALUES
        (#{boardType}
        ,(SELECT NVL(MAX(BOARD_NUM)+1,1)
        FROM BOARD)
        ,#{boardTitle}
        ,#{boardComment}
        ,'SYSTEM'
        ,TO_CHAR(SYSDATE)
        ,'SYSTEM'
        ,TO_CHAR(SYSDATE)
        )
    </select>
    
    <delete id = "boardDelete" parameterType="com.spring.board.vo.BoardVo">
    	DELETE FROM BOARD 
    	 WHERE  BOARD_NUM = #{boardNum}
    	 AND BOARD_TYPE = #{boardType}
    </delete>
    
    <update id = "boardUpdate" parameterType="com.spring.board.vo.BoardVo">
    UPDATE BOARD 
    SET
    BOARD_TITLE = #{boardTitle},
     BOARD_COMMENT=#{boardComment},
     MODIFIED_TIME=TO_CHAR(SYSDATE) 
     WHERE BOARD_NUM = #{boardNum}
     AND BOARD_TYPE = #{boardType}
    </update>
    
    
      <select id="menuList" resultMap="boardVo">
      	SELECT * FROM
      	COM_CODE
      	WHERE 
      	CODE_TYPE = 'menu'
      </select>
      
      
      <select id="boardListSearch" parameterType="com.spring.board.vo.PageVo" resultMap="boardVo">
        SELECT 
		  BOARD_TYPE
		  ,BOARD_NUM
		  ,BOARD_TITLE
		  ,BOARD_COMMENT
		  ,TOTAL_CNT
		  ,CODE_NAME
		FROM 
		(
		  SELECT
		      	BOARD_TYPE
				,BOARD_NUM
				,BOARD_TITLE
				,BOARD_COMMENT
		      	,ROW_NUMBER() OVER(ORDER BY BOARD_NUM DESC) AS NUMROW
		      	,COUNT(*) OVER() AS TOTAL_CNT
		      	,CASE BOARD_TYPE 
		      	WHEN 'a01' THEN 
		      	(SELECT CODE_NAME FROM COM_CODE WHERE CODE_ID = 'a01' AND CODE_TYPE= 'menu')
		      	WHEN 'a02' THEN 
		      	(SELECT CODE_NAME FROM COM_CODE WHERE CODE_ID = 'a02' AND CODE_TYPE= 'menu')
		      	WHEN 'a03' THEN 
		      	(SELECT CODE_NAME FROM COM_CODE WHERE CODE_ID = 'a03' AND CODE_TYPE= 'menu')
		      	WHEN 'a04' THEN 
		      	(SELECT CODE_NAME FROM COM_CODE WHERE CODE_ID = 'a04' AND CODE_TYPE= 'menu')
				END 
				CODE_NAME
			FROM
				BOARD	
		)
		
		WHERE
		 
		NUMROW BETWEEN (#{pageNo}-1)*10 + 1 AND (#{pageNo})*10
		AND 
		<foreach item="item" collection="boardTypeArr" separator="or">
		BOARD_TYPE = #{item} 
		</foreach>
    </select>
      
    
</mapper>
